import { defineClientConfig } from "vuepress/client";
import RightPromoBar from "./components/RightPromoBar.vue";

export default defineClientConfig({
  enhance() {},
  setup() {},
  rootComponents: [RightPromoBar],
});
